# nelson_siegel_curve_project
bond yield curve

$`\alpha + \beta`$.
$`y(r) = \beta_0 + \beta_1 (\frac{(1 - e^r )}{( \lambda * T )} ) + B_2 * ((1 - np.exp(exp_input))/(L * T) - np.exp(exp_input)) + B_3 * ((1 - np.exp(mu_input))/(mu * T) - np.exp(mu_input)`$

<img width="1170" height="908" alt="image" src="https://github.com/user-attachments/assets/bf52634e-1c02-4432-8266-2838a7847166" />
